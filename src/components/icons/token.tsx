import * as React from "react";
import { SVGProps } from "react";

const IconToken = (props: SVGProps<SVGSVGElement>) => (
  <svg {...props} xmlns="http://www.w3.org/2000/svg" width={19} height={18} fill="none">
    <circle cx={9.931} cy={9.001} r={9} fill="currentColor" />
  </svg>
);
export default IconToken;
