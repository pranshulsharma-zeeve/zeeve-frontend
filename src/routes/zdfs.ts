const PAGE = {
  PURCHASE: "/purchase-subscription",
  FILES: "/files",
  GATEWAY: "/gateway",
  FILE_CID_VERIFIER: "/file-cid-verifier",
  ANALYTICS: "/analytics",
};

const API = {};

const ZDFS_ROUTES = {
  PAGE,
  API,
};

export default ZDFS_ROUTES;
