apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-front-2-0.fullname" . }}-test-connection"
  labels:
    {{- include "app-front-2-0.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-front-2-0.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
