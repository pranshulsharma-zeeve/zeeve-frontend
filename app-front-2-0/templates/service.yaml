apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-front-2-0.fullname" . }}
  labels:
    {{- include "app-front-2-0.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-front-2-0.selectorLabels" . | nindent 4 }}
